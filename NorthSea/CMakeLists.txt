set( GETM_USE_STATIC ON )
set( CMAKE_INSTALL_PREFIX ${PROJECT_SOURCE_DIR}/NorthSea )
set( GETM_COORDINATE_TYPE Spherical )

if ( NOT ${GETM_USE_PARALLEL} )
    set(IEXTR 112)
    set(JEXTR 125)
    set(IMAX  112)
    set(JMAX  125)
    set(KMAX   25)
else()
    set(IEXTR 112)
    set(JEXTR 125)
    set(IMAX   56)
    set(JMAX   63)
    set(KMAX   25)
endif()

if ( NOT ${GETM_USE_PARALLEL} )
    set(IEXTR 202)
    set(JEXTR   4)
    set(IMAX  202)
    set(JMAX    4)
    set(KMAX   25)
else()
    set(IEXTR 202)
    set(JEXTR   4)
    set(IMAX   52)
    set(JMAX    4)
    set(KMAX   25)
endif()
set(GETM_COORDINATE_TYPE Cartesian)

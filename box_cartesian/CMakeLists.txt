set( GETM_USE_STATIC ON )
set( CMAKE_INSTALL_PREFIX ${PROJECT_SOURCE_DIR}/box_cartesian )
set( GETM_COORDINATE_TYPE Cartesian )

if ( NOT ${GETM_USE_PARALLEL} )
    set(IEXTR 100)
    set(JEXTR  30)
    set(IMAX  100)
    set(JMAX   30)
    set(KMAX   30)
else()
    set(IEXTR 100)
    set(JEXTR  30)
    set(IMAX   50)
    set(JMAX   15)
    set(KMAX   30)
endif()

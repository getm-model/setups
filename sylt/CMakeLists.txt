set( GETM_USE_STATIC ON )
set( CMAKE_INSTALL_PREFIX ${PROJECT_SOURCE_DIR}/sylt )
set( GETM_COORDINATE_TYPE Cartesian )

message(${GETM_USE_PARALLEL})

if ( NOT ${GETM_USE_PARALLEL} )
    set(IEXTR 135)
    set(JEXTR 160)
    set(IMAX  135)
    set(JMAX  160)
    set(KMAX   20)
else()
    set(IEXTR 135)
    set(JEXTR 160)
    set(IMAX   68)
    set(JMAX   80)
    set(KMAX   20)
endif()

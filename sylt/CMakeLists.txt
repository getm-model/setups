if ( NOT ${GETM_USE_PARALLEL} )
    set(IEXTR 135)
    set(JEXTR 160)
    set(IMAX  135)
    set(JMAX  160)
    set(KMAX   25)
else()
    set(IEXTR 135)
    set(JEXTR 160)
    set(IMAX   68)
    set(JMAX   80)
    set(KMAX   25)
endif()
set(GETM_COORDINATE_TYPE Cartesian)
